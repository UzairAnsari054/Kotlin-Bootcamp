package com.example.kotlinbootcamp

/*

val is used for variables which are immutable (not changeable). That means Val cannot be reassigned.
var is used for variables which are mutable/changeable.

*/